name: apm
version: 1.0.0
description: APM project for apm
author: TAKE Takashi
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: copilot, claude, cursor, opencode, codex, gemini, windsurf, agent-skills, all
targets:
- agent-skills
dependencies:
  apm: []
  mcp: []
includes: auto
scripts: {}
